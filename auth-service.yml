server:
  port: 8084

spring:
  datasource:
    username: chinmayanand
    password: admin

  jpa:
    hibernate:
      ddl-auto: update

  mail:
    host: smtp.gmail.com
    port: 587
    # Set via the MAIL_USERNAME / MAIL_PASSWORD env vars on whatever runs this
    # service - never commit real credentials here.
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: 1CHN1Bi8R0YYkCN6mrDEcys4HHAK3RJqU/1Alp+B1Xg=
  expiration: 36000000