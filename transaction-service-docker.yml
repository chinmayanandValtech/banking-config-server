spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/transaction_db

  # Kafka disabled for now (transaction-service no longer publishes MoneyTransferredEvent).
  # Uncomment to re-enable once Kafka publishing is wired back up.
  # kafka:
  #   bootstrap-servers: kafka:9092
  #
  #   producer:
  #     key-serializer: org.apache.kafka.common.serialization.StringSerializer
  #     value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
  #
  #   consumer:
  #     group-id: account-group
  #     auto-offset-reset: earliest
  #     key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
  #     value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
  #
  #     properties:
  #       spring:
  #         json:
  #           trusted:
  #             packages: "*"
  #           value:
  #             default:
  #               type: com.bank.transaction_service.event.MoneyTransferredEvent
